{
  "family": "btg-{{ENVIRONMENT}}-enhancer-service",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "512",
  "memory": "1024",
  "executionRoleArn": "arn:aws:iam::{{AWS_ACCOUNT_ID}}:role/btg-{{ENVIRONMENT}}-enhancer-service-execution-role",
  "taskRoleArn": "arn:aws:iam::{{AWS_ACCOUNT_ID}}:role/btg-{{ENVIRONMENT}}-enhancer-service-task-role",
  "containerDefinitions": [
    {
      "name": "enhancer-service",
      "image": "ghcr.io/btg-c/btg-enhancer-service:{{IMAGE_TAG}}",
      "essential": true,
      "portMappings": [
        {
          "containerPort": 8080,
          "protocol": "tcp"
        }
      ],
      "environment": [],
      "secrets": [
        {
          "name": "MONGODB_URI",
          "valueFrom": "arn:aws:secretsmanager:{{AWS_REGION}}:{{AWS_ACCOUNT_ID}}:secret:btg/{{ENVIRONMENT}}/mongodb-uri"
        },
        {
          "name": "SCORE_ODD_API_KEY",
          "valueFrom": "arn:aws:secretsmanager:{{AWS_REGION}}:{{AWS_ACCOUNT_ID}}:secret:btg/{{ENVIRONMENT}}/score-odd-api-key"
        }
      ],
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/btg-{{ENVIRONMENT}}-enhancer-service",
          "awslogs-region": "{{AWS_REGION}}",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 60
      }
    }
  ]
}
